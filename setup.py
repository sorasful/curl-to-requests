from setuptools import setup

setup(
    name='Curl to requests',
    version='0.1',
    url='https://github.com/sorasful/curl-to-requests/',
    license='MIT',
    description='Module to convert a curl command copied from Chrome into a requests syntaxe equivalent.',
    platforms='any',
    )